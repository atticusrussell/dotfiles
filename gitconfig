[init]
	defaultBranch = main

# enables overriding of this config with a local config
[include]
        path = ~/.gitconfig_local
[push]
	default = current
[core]
	excludesfile = /home/atticus/.gitignore_global
